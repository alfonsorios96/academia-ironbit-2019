import { css } from 'lit-element';

export default css`
*, *:before, *:after {
  box-sizing: inherit;
  font-family: inherit; }

paper-dialog {
  display: flex;
  flex-direction: column;
  overflow: scroll; }
`;
